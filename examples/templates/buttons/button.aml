let THEME = [
    // Inactive
    {
        "background": "black",
        "foreground": "reset",
    },

    // Active
    {
        "background": "grey",
        "foreground": "black",
    }
]


border
    row
        text [background: THEME[state.active].background, foreground: THEME[state.active].foreground] attributes.caption
