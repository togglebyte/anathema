vstack
    // Output
    expand
        border
            expand
                vstack
                    // Iterate over the output
                    // and output the text
                    for s in output
                        text s

    // Input
    border
        expand [axis: "horz"]
            text [foreground: #00aaaa] input
                // using a span to add a cursor at the end
                span "█"
